{
	"$schema": "../gen/schemas/mobile-schema.json",
	"identifier": "mobile-capability",
	"description": "Mobile webview. Read-only fs access scoped to the only two sources a document can come from: the system picker (content:// URIs) and the app's managed imports store where open-with payloads are copied. A parser bug can therefore not read arbitrary app-private files over IPC. No write access, no network.",
	"platforms": ["android", "iOS"],
	"windows": ["main"],
	"permissions": [
		"core:default",
		"dialog:allow-open",
		{
			"identifier": "fs:default",
			"allow": [
				{ "path": "content://**" },
				{ "path": "$APPDATA/imports/**" },
				{ "path": "$APPDATA/imports/*" }
			]
		}
	]
}
